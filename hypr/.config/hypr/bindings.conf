# See https://wiki.hypr.land/Configuring/Keywords/ 
$mainMod = SUPER # Sets "Windows" key as main modifier 

$terminal = kitty 
$menu = rofi -show drun  # TODO: switch to wofi or walker?

bind = $mainMod, return, exec, $terminal 
bind = $mainMod, space, exec, $menu 

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1 

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow 

# Laptop multimedia keys for volume 
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Swap window with mainMod + arrow keys
bind = $mainMod CONTROL, left, swapwindow, l
bind = $mainMod CONTROL, right, swapwindow, r
bind = $mainMod CONTROL, up, swapwindow, u
bind = $mainMod CONTROL, down, swapwindow, d

# Move focus with mainMod + neio
bind = $mainMod, N, movefocus, l
bind = $mainMod, E, movefocus, d
bind = $mainMod, I, movefocus, u
bind = $mainMod, O, movefocus, r

# Swap window with mainMod + neio
bind = $mainMod CONTROL, N, swapwindow, l
bind = $mainMod CONTROL, E, swapwindow, d
bind = $mainMod CONTROL, I, swapwindow, u
bind = $mainMod CONTROL, O, swapwindow, r

bindd = $mainMod, Q, Switch to workspace Q, workspace, name:Q
bindd = $mainMod, F, Switch to workspace F, workspace, name:F
bindd = $mainMod, B, Switch to workspace B, workspace, name:B
bindd = $mainMod, A, Switch to workspace A, workspace, name:A
bindd = $mainMod, R, Switch to workspace R, workspace, name:R
bindd = $mainMod, S, Switch to workspace S, workspace, name:S
bindd = $mainMod, T, Switch to workspace T, workspace, name:T
bindd = $mainMod, G, Switch to workspace G, workspace, name:G
bindd = $mainMod, Z, Switch to workspace Z, workspace, name:Z
bindd = $mainMod, X, Switch to workspace X, workspace, name:X
bindd = $mainMod, D, Switch to workspace D, workspace, name:D
bindd = $mainMod, L, Switch to workspace L, workspace, name:L
bindd = $mainMod, U, Switch to workspace U, workspace, name:U
bindd = $mainMod, Y, Switch to workspace Y, workspace, name:Y
bindd = $mainMod, M, Switch to workspace M, workspace, name:M
bindd = $mainMod, H, Switch to workspace H, workspace, name:H

bindd = $mainMod CONTROL, Q, Move window to workspace Q, movetoworkspace, name:Q
bindd = $mainMod CONTROL, F, Move window to workspace F, movetoworkspace, name:F
bindd = $mainMod CONTROL, B, Move window to workspace B, movetoworkspace, name:B
bindd = $mainMod CONTROL, A, Move window to workspace A, movetoworkspace, name:A
bindd = $mainMod CONTROL, R, Move window to workspace R, movetoworkspace, name:R
bindd = $mainMod CONTROL, S, Move window to workspace S, movetoworkspace, name:S
bindd = $mainMod CONTROL, T, Move window to workspace T, movetoworkspace, name:T
bindd = $mainMod CONTROL, G, Move window to workspace G, movetoworkspace, name:G
bindd = $mainMod CONTROL, Z, Move window to workspace Z, movetoworkspace, name:Z
bindd = $mainMod CONTROL, X, Move window to workspace X, movetoworkspace, name:X
bindd = $mainMod CONTROL, D, Move window to workspace D, movetoworkspace, name:D
bindd = $mainMod CONTROL, L, Move window to workspace L, movetoworkspace, name:L
bindd = $mainMod CONTROL, U, Move window to workspace U, movetoworkspace, name:U
bindd = $mainMod CONTROL, Y, Move window to workspace Y, movetoworkspace, name:Y
bindd = $mainMod CONTROL, M, Move window to workspace M, movetoworkspace, name:M
bindd = $mainMod CONTROL, H, Move window to workspace H, movetoworkspace, name:H
